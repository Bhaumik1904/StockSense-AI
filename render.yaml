services:
  - type: web
    name: stocksense-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
